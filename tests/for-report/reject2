package main;


func ayam(){

	i, j := 0, 10
	f := func() int { return 7 }
	ch := make(chan int)
	r, w := os.Pipe(fd)  // os.Pipe() returns two values
	_, y, _ := coord(p) 

	var d = math.Sin(0.5) + 2.5i )
}